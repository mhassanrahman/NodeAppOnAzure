extends layout

block content
  h1= title
  p Welcome to #{title}
  each book in books
    div
      div
        img(src="https://librarynodeapp.blob.core.windows.net/covers/" + book.bookId + ".jpg")
      h3=book.title
      h4=book.author
